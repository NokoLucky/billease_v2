workflows:
  ios_testflight:
    name: iOS TestFlight (Next.js + Capacitor)
    max_build_duration: 120

    environment:
      node: "18"
      cocoapods: default

      vars:
        APPLE_ID: nokolucky19@gmail.com
        APPLE_KEY_ID: $APPSTORE_CONNECT_KEY_ID
        APPLE_ISSUER_ID: $APP_STORE_CONNECT_ISSUER_ID
        APPLE_PRIVATE_KEY: $APPSTORE_CONNECT_PRIVATE_KEY

      ios_signing:
        distribution_type: app-store
        bundle_identifier: com.billease.app.UNC2YV5NU9

    scripts:
      - name: Install dependencies
        script: |
          npm ci

      - name: Build Next.js web assets
        script: |
          npm run build

      - name: Sync Capacitor iOS
        script: |
          npx cap sync ios

      - name: Install CocoaPods
        script: |
          cd ios/App
          pod install --repo-update
          cd ../..

      - name: Archive iOS build with Xcode
        script: |
          set -e
          cd ios/App

          xcodebuild -workspace App.xcworkspace \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            -archivePath $CM_BUILD_DIR/App.xcarchive archive

          xcodebuild -exportArchive \
            -archivePath $CM_BUILD_DIR/App.xcarchive \
            -exportPath $CM_BUILD_DIR/ipa \
            -exportOptionsPlist $CM_PROJECT_DIR/export-options.plist

    artifacts:
      - $CM_BUILD_DIR/ipa/*.ipa

    publishing:
      app_store_connect:
        api_key: $APPLE_PRIVATE_KEY
        key_id: $APPLE_KEY_ID
        issuer_id: $APPLE_ISSUER_ID
        apple_id: $APPLE_ID
        submit_to_testflight: true
